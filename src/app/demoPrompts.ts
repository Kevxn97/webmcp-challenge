export const PLANNING_MISSION_PROMPT = `Read the current factory. Create and simulate two one-shift scenarios using the revisions returned by each call. Scenario A: mixer 9500 bps, Packaging 9000 bps, 15-minute changeover, enhanced calibration, expedited supplier. Scenario B: the same line settings with the standard supplier. Do not add machines. Compare the stored receipts.`;

export const HUMAN_INTERVENTION_PROMPT = `Retry the last Scenario B write once with a fresh request ID but the exact revisions you held before my click. Do not re-read first. Report the structured error and confirm that operational and scenario state did not commit. Then re-read the factory, create a fresh scenario under the new human authority, keep Packaging unchanged, apply only controls reported as AVAILABLE, simulate one shift, and explain the deterministic evidence.`;
